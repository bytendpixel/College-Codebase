#include<stdio.h>
int main()

// Area of a sqare

{
    float side;
    printf("Enter the side : ");
    scanf("%f",&side);
    float area = side*side;
    printf("The area of a square is : %f", area);
    return 0;
}